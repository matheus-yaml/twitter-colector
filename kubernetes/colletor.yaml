apiVersion: apps/v1
kind: Deployment
metadata:
  name: collector
spec:
  replicas: 1

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0

  selector: 
    matchLabels: 
      app: collector
  template:
    metadata:
      labels:
        app: collector
    spec:
      restartPolicy: Always

      containers:
        - name: collector
          image: fernandogbi/twitter-collector:0.1.1
          env:
            - name: API_KEY
              value: 
            - name: API_SECRET_KEY
              value: 
            - name: ACCESS_TOKEN
              value: 
            - name: ACCESS_TOKEN_SECRET
              value: 

              resources:
                limits:
                  memory: "300Mi"
                  cpu: "200m"





